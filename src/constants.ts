import { SpringOptions } from "@rbxts/ripple";

export const TOGGLE_DEFAULT_HEIGHT = 32;
export const BUTTON_DEFAULT_HEIGHT = 32;
export const TAB_BUTTON_DEFAULT_HEIGHT = 32;
export const DEFAULT_FONT_NORMAL = Enum.Font.SourceSans;
export const DEFAULT_TEXT_SIZE = 18;
export const DEFAULT_ICON_SIZE = new Vector2(16, 16);
export const SPRING_FAST: SpringOptions = {
	tension: 500,
	damping: 1,
	friction: 0,
};
